/*
a.Nested routes
the use of nested routes is where we can use a foler X nesting folder Y. Eg X/Y

b.Dynamic routes
it has a [] because if we ya x/y/z/a/b/c its curbusome so to prevent we use [] for dynamic elements
-- params prop is used to access a something eg productId
 */
